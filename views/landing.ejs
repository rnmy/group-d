<%- include("./partials/header") %>
  <div class="container">
    <% if(!currentUser) { %>
    <h1 class="display-4 text-center mt-5 mb-3">Welcome to <strong>group'd!</strong></h1>
    <div class="container">
      <div class="row">
        <div class="col text-center">
          <div class="form-group">
            <a href="/login"><button class="btn btn-info form-control" style="width: 300px;">Login</button></a>
          </div>
          <div class="form-group">
            <button class="btn btn-link form-control"><a href="/register">Don't have an account? Register with us.</a></button>
          </div>
        </div>
      </div>
    </div>
    <% } else { %>
      <h1 class="display-4 text-center mt-5 mb-3">You are currently using <strong>group'd!</strong></h1>
    <% } %>
  </div>

<%- include("./partials/footer") %>
